import React from 'react';
import PropTypes from 'prop-types';
import { View } from 'react-native';

const TemplesScreen = props => {
    return (
      <View></View>
    );
};

TemplesScreen.propTypes = {
    
};

export default TemplesScreen;